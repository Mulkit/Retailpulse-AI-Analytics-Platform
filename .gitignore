# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Large raw data — not needed for the deployed dashboard (regenerate via notebooks/01_eda.ipynb)
data/raw/sales_transactions.csv
data/raw/customers_master.csv
data/raw/inventory_snapshots.csv

# Model binaries — not read by the dashboard, only by training notebooks
models/*.pt
models/*.joblib
models/prophet_model.json
models/churn_xgboost.json
models/churn_xgboost_tuned.json

# MLflow
mlruns/
notebooks/mlruns/

# Environment
.env
venv/
.venv/

# OS
.DS_Store